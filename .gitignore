# Ignore all sede directories EXCEPT demo/ (which serves as the canonical example).
# The fetch-sedes script generates these from the DB; they are environment data.
/sedes/*
!/sedes/demo/
!/sedes/demo/**

# Node modules (if installed locally)
node_modules/

# Environment files
.env
.env.local

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# (Nota: scripts/lib/backend-source/ NO se ignora — es el context codebase
#  que los agentes leen para diagnosticar leads. Se incluye en git.)
